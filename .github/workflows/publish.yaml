---
name: publish package

on:
  push:
    tags:
      - "v*.*.*"
      - "*.*.*"
    paths-ignore:
      - .github/**

jobs:
  publish:
    name: publish package
    uses: strg-at/github-workflows/.github/workflows/npm-publish.yaml@34cd70de188f32bb7da46b319566aed03777c270 # v2.1.6
    with:
      runner: '["ubuntu-latest"]'
