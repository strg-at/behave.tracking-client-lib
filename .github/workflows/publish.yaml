---
name: publish package

on:
  push:
    tags:
      - "v*.*.*"
      - "*.*.*"
    paths-ignore:
      - .github/**

jobs:
  publish:
    name: publish package
    uses: strg-at/github-workflows/.github/workflows/npm-publish.yaml@9152079a0484462016685fa923cdf0da57cdd597 # v1.11.3
    with:
      runner: '["ubuntu-latest"]'
