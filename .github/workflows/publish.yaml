---
name: publish package

on:
  push:
    tags:
      - "v*.*.*"
      - "*.*.*"
    paths-ignore:
      - .github/**

jobs:
  publish:
    name: publish package
    uses: strg-at/github-workflows/.github/workflows/npm-publish.yaml@3d7b59957f65c08a3e2b5d3228ee3744bbcb72a7 # v2.1.9
    with:
      runner: '["ubuntu-latest"]'
