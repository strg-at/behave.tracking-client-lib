---
name: publish package

on:
  push:
    tags:
      - "v*.*.*"
      - "*.*.*"
    paths-ignore:
      - .github/**

jobs:
  publish:
    name: publish package
    uses: strg-at/github-workflows/.github/workflows/npm-publish.yaml@ee3733899572f6a1de4a2b467e854bb0b2882674 # v2.0.0
    with:
      runner: '["ubuntu-latest"]'
