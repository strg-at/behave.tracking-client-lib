---
name: publish package

on:
  push:
    tags:
      - "v*.*.*"
      - "*.*.*"
    paths-ignore:
      - .github/**

jobs:
  publish:
    name: publish package
    uses: strg-at/github-workflows/.github/workflows/npm-publish.yaml@d5ae55933aa47698c75656e4fdd8b83f81026e56 # v1.11.2
    with:
      runner: '["ubuntu-latest"]'
