---
name: publish package

on:
  push:
    tags:
      - "v*.*.*"
      - "*.*.*"
    paths-ignore:
      - .github/**

jobs:
  publish:
    name: publish package
    uses: strg-at/github-workflows/.github/workflows/npm-publish.yaml@42c94293164b81d878edbf35a402a246c102a196 # v2.2.1
    with:
      runner: '["ubuntu-latest"]'
